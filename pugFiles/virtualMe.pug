doctype html

html
	include Templates/header.pug

script(type='text/javascript').
	alert(window.location.hash);

	$.post('/requestFitbit',
	{
		postData: window.location.hash
	},
	function(data, status){
		alert('sent');
	});

